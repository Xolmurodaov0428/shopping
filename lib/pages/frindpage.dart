import 'package:flutter/material.dart';

class FrindPage extends StatelessWidget {
  const FrindPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Expanded(child: Container(color: Colors.amberAccent[100])),
      ),
    );
  }
}
